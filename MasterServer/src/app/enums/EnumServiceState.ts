export enum EnumServiceState {
    normal = "normal", //**服务正常 */

    busy = "busy", //**服务繁忙 */

    stop = "stop", //**服务已停止 */

    error = "error", //**服务出错了 */,
    
    unknow = "unknow" //**未知状态 */
    
}